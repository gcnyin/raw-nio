plugins {
  id 'java'
  id 'application'
  id 'jacoco'
}

repositories {
  mavenLocal()
  mavenCentral()
}

dependencies {
  testImplementation 'junit:junit:4.13'
}

application {
  mainClassName = 'com.github.gcnyin.rawnio.http1.HttpServer'
}

jar {
  manifest {
    attributes("Main-Class": "com.github.gcnyin.rawnio.http1.HttpServer")
  }
}

test {
  testLogging.showStandardStreams = true
  finalizedBy jacocoTestReport // report is always generated after tests run
}

jacocoTestReport {
  dependsOn test // tests are required to run before generating the report
}
